# ドッグフーディング優先ロードマップ 提案

作成日: 2026-07-20
前提: 移植仕様書にセクション5「実績記録」を入れる方針が承認された
目的: この移植プロジェクト自体を PSP で計測し、そのデータを Processloop に投入する
位置づけ: **提案。未確定。** 実装順序の変更を伴うため、採否の判断を仰いでいる段階である

## 既存文書との関係

[deliverables-proposal.md](deliverables-proposal.md) は**何を作るか**（18ユニット、成果物、工程）を定義する。
本書は**どの順で作るか**を扱い、成果物の定義そのものは変更しない。

本書を採用する場合、`deliverables-proposal.md` の第3章「第1期の内部分割（第1期a / 第1期b）」を、
本書の M1〜M6 で置き換えることになる。採用しない場合は現行の第1期a/b が維持される。

---

## 1. 結論

**実装順序を「計算エンジン先行」から「データ収集先行」に変更することを提案する。**

理由は次章の1点に尽きるが、先に結論の形を示す。

| | 現行案 | 提案 |
|---|---|---|
| 最初に作るもの | A群（計算エンジン7ユニット） | データ収集の最小構成 |
| 最初に動くもの | なし（ライブラリのみ） | **時間を記録できるツール** |
| ドッグフーディング開始 | 第1期の終盤 | **約2,150行の時点** |
| 手動記録の期間 | 第1期の全期間 | MVP 完成まで |

**重要**: この変更で捨てる作業は1行もない。すべて第1期スコープ内の作業であり、**順序を入れ替えるだけ**である。

---

## 2. 決定的な理由：PROBE は履歴データがないと成立しない

PROBE（PROxy Based Estimating）は、**過去の実績データに回帰分析をかけて見積もる手法**である。

```
過去の「見積り規模 vs 実績規模」の組 → 線形回帰 → 次の見積り
```

つまり **PROBE を実装しても、履歴データがなければ何も出力できない**。

現行案では、PROBE（B-6）を実装する頃には第1期の大半が終わっている。そこから記録を始めても、**自分のプロジェクトで PROBE を検証する機会がない**。

対してデータ収集を先行させれば、B-6 に到達した時点で**十数ユニット分の実データ**が溜まっている。回帰分析が実際に意味を持つ。

同じことが EV（B-7/B-8）にも言える。計画対実績の比較は、計画と実績の両方が記録されていて初めて成立する。

### 副次的な理由

**1. データモデルの早期検証**

`HierarchyNode` / `TimeLogEntry` / `Defect` の設計が実用に耐えるかは、使ってみないと分からない。終盤で判明すると手戻りが大きい。

**2. 動機の持続**

7,200〜9,800行は長い道のりである。途中で自分のデータが見えるものがあると続けやすい。

**3. ツール自体の説得力**

「PSP のツールを作ったが、開発中は PSP を使っていない」という状態は避けたい。

---

## 3. 改訂ロードマップ

### M1: データ収集の最小構成（約2,150行）★ドッグフーディング開始点

**到達点**：階層を作り、タスクの作業時間を記録できる。

| ユニット | 内容 | 行数 |
|---|---|---|
| B-9（部分） | Prisma スキーマ（HierarchyNode, TimeLogEntry） | 約250 |
| B-2（部分） | 階層の生成・編集・パス解決 | 約400 |
| B-3（部分） | `PSP-template.xml` からフェーズ一覧を読む | 約200 |
| B-4 | 時間ログ（計測・中断・記録） | 約500 |
| C（部分） | 階層画面・タイマー画面 | 約800 |

**この時点で計算エンジンは不要である。** 表示するのは合計時間などの単純な集計のみで、TypeScript で直接書ける（約100行）。

PSP の派生指標（歩留まり、欠陥密度、CPI/SPI）はエンジンが担うが、**生データの記録は独立している**。ここが順序を入れ替えられる根拠になる。

### M2: 欠陥ログ（約1,000行）

**到達点**：欠陥を記録し、フェーズ別の件数が見える。

| ユニット | 内容 | 行数 |
|---|---|---|
| B-5 | 欠陥ログ（混入・除去フェーズ） | 約600 |
| C（部分） | 欠陥ログ画面 | 約400 |

M1 と M2 で、**PSP の3大生データ（時間・欠陥・規模）がすべて記録できる**。

### M3: 計算エンジン（約5,000行）

**到達点**：PSP の派生指標が自動計算される。

```
A-1 プリプロセッサ ✅ → A-3 値の型 → A-4 パーサ → A-5 評価器
  → A-2 動的マクロ → A-7 DataRepository → A-6 組み込み関数
  → B-1 util → B-3（残り） → B-10 Templates変換
```

M1・M2 で溜めた実データに、**PSP の計算式1.2万行が適用される**。ここで初めて歩留まりや欠陥密度が見える。

### M4: PROBE（約1,200行）

**到達点**：溜まった履歴データで見積もれる。

M1 から M3 までの期間で記録した**十数ユニット分のデータ**が、回帰分析の入力になる。

### M5: EV（約3,000行）

**到達点**：計画対実績、完了予測。

### M6: 仕上げ（約1,000行）

サマリ画面、E2E テスト、ユーザーマニュアル、デプロイ。

---

## 4. 手動記録フェーズ（今日から開始）

M1 完成までは手作業で記録する。**フォーマットを Prisma スキーマに合わせておけば、後で一括投入できる**。

### 配置

```
docs/psp-data/
├─ time-log.csv
├─ defect-log.csv
└─ size-log.csv
```

### 時間ログ

```csv
unit,phase,date,start,end,delta_min,interrupt_min,comment
A-2,解析,2026-07-21,09:00,10:30,80,10,TemplateAutoData の読解
A-2,設計,2026-07-21,10:40,11:20,40,0,型設計
```

`delta_min` は**中断を除いた正味時間**である。PSP の本質はここにあるため、`interrupt_min` を別列にしている。

### 欠陥ログ

```csv
unit,found_phase,injected_phase,type,fix_min,description
A-1,単体テスト,実装,80 Function,25,BOM でゴールデンが壊れた
A-1,実装,設計,50 Interface,10,@types/node のバージョン不整合
```

`type` は **PSP 標準の欠陥タイプ10種**を使う。

```
10 Documentation   20 Syntax        30 Build/Package   40 Assignment
50 Interface       60 Checking      70 Data            80 Function
90 System         100 Environment
```

### 規模ログ

```csv
unit,estimated_loc,actual_loc,est_date,complete_date
A-1,300,315,2026-07-19,2026-07-20
```

**この規模データが PROBE の入力になる。** 見積りと実績の組が10件も溜まれば、回帰分析が意味を持ち始める。

---

## 5. A-1 の遡及記録（既に材料がある）

**devlog に PSP データの原型が既に記録されている。** 抽出すれば A-1 を遡及登録できる。

### 規模

| 項目 | 値 | 出所 |
|---|---|---|
| 見積り | 250〜350行 | ロードマップの推定 |
| 実績 | 315行 | 実装完了時の実測 |

**見積りが的中している**（範囲の中央付近）。これは1件目の実データになる。

### 欠陥（devlog のつまずき記録から）

| 混入 | 発見 | タイプ | 内容 |
|---|---|---|---|
| 実装 | 単体テスト | 80 Function | BOM でゴールデンが壊れた |
| 実装 | 実装 | 100 Environment | `@types/node` が実行環境と不一致 |
| 実装 | 実装 | 20 Syntax | `package.json` の JSON 構文エラー |
| 設計 | 実装 | 50 Interface | 相対パスの階層数の誤り |

**4件の欠陥が記録済み**である。混入フェーズと発見フェーズの対応も分かる。

### 所要時間

⚠️ ここだけが**再現できない**。会話の記録からおおよその区間は分かるが、正味の作業時間は不明である。

**A-1 は「規模と欠陥のみ」で登録し、時間は空欄とする**のが誠実だと考える。捏造した数値を入れると、PROBE の回帰が歪む。

---

## 6. 開発工程と PSP フェーズの対応

⑤で定義された工程が、**PSP のフェーズにほぼそのまま対応する**。これは偶然ではなく、どちらもソフトウェア開発の工程を表しているためである。

| Processloop の工程 | PSP フェーズ | type |
|---|---|---|
| 移植元の解析 | Planning | plan |
| プログラム設計 | Design | dld |
| （設計レビュー） | Design Review | dldr（appraisal） |
| 実装 | Code | code |
| （コードレビュー） | Code Review | cr（appraisal） |
| 型チェック（`pnpm typecheck`） | Compile | comp（failure） |
| 単体テスト | Test | ut（failure） |
| 事後分析 | Postmortem | pm |

**`pnpm typecheck` が PSP の Compile に対応する**のが面白い点である。型エラーは「コンパイル時に見つかった欠陥」として記録できる。

### 階層の構成案

```
Processloop 第1期
├─ A-1 プリプロセッサ
│  ├─ 解析 / 設計 / 実装 / 型チェック / 単体テスト / 事後分析
├─ A-2 動的マクロ生成
│  └─ （同上）
├─ B-2 hier
└─ …（18ユニット）
```

**ユニット＝タスク、工程＝フェーズ**という対応で、PSP の構造にきれいに収まる。

---

## 7. リスクと対処

### リスク1：UI が計算エンジン導入時に手戻りする

M1 で作る画面は、エンジン導入後に表示内容が変わる可能性がある。

**対処**：M1 の集計を**エンジンで置き換えやすい形**に隔離する。

```typescript
// M1: 直接計算
export function totalTime(entries: TimeLogEntry[]): number { … }

// M3 以降: エンジン経由に差し替え
export function totalTime(path: string): number {
  return repository.getValue(`${path}/Time`);
}
```

呼び出し側（UI）のインタフェースを変えなければ、差し替えは局所的で済む。

### リスク2：文脈の切り替えコスト

A-1 でエンジンの調査・実装に集中していたため、UI 側に移ると頭を切り替える必要がある。

**対処**：調査済みの資産は消えない。`grammar.txt` の分析も Signals の判断も、M3 でそのまま使える。**保留になるだけで無駄にはならない**。

### リスク3：記録の手間が続かない

手動記録は面倒である。

**対処**：M1 を早く終わらせることが最大の対策になる。それまでは**粒度を粗くする**（1日1行でもよい）。完璧な記録より、続く記録の方が価値がある。

---

## 8. 保留中の質問への影響

この提案を採用する場合、以前の3つの質問のうち Q3 に答えが出る。

| # | 質問 | この提案での答え |
|---|---|---|
| Q1 | 文書の粒度 | 変更なし（25本案のまま） |
| Q2 | A-1 の遡及文書 | **規模と欠陥は遡及可能**（devlog に材料あり）。時間は不明として空欄 |
| Q3 | frontend の初期化時期 | **今すぐ**（M1 に含まれるため） |

---

## 9. 現行案との比較

| 観点 | 現行案（エンジン先行） | 提案（データ収集先行） |
|---|---|---|
| 最初に動くもの | なし | **時間計測ツール** |
| PROBE 検証時の履歴 | ほぼゼロ | **十数ユニット分** |
| データモデルの検証 | 終盤 | **早期** |
| 手動記録の期間 | 第1期の全期間 | M1 まで |
| 総実装量 | 変わらない | 変わらない |
| 捨てる作業 | — | **なし（順序変更のみ）** |

---

## 10. 直近の行動案

**1. 今日から手動記録を開始する**（コスト：ほぼゼロ）

`docs/psp-data/` に3つの CSV を置き、A-1 を遡及登録する。

**2. A-2 以降は記録しながら進める**

**3. M1 に着手する**

```
frontend 初期化 → Prisma スキーマ → hier 最小構成
  → 時間ログ → 階層画面 → タイマー画面
```

---

## 11. 判断を仰ぎたい点

**順序変更そのものへの賛否**が最大の論点である。

計算エンジンの調査（`grammar.txt` 28規則、Signals の採用判断、Peggy の選定）を積み上げてきた直後に、UI 側へ移ることになる。**勢いを断つ**という見方もありうる。

代案として、次の折衷案も考えられる。

**折衷案：A-3（値の型システム）まで進めてから M1 へ**

A-3 は約600行で、全体の土台になる型定義である。ここまで終えてから MVP に移れば、エンジンの基礎が固まった状態で中断できる。

| 案 | ドッグフーディング開始 | エンジンの区切り |
|---|---|---|
| 提案（即 M1） | 最速 | A-1 で中断 |
| 折衷（A-3 の後） | 約600行遅れ | 型システムまで完成 |
| 現行維持 | 最も遅い | 途切れない |
